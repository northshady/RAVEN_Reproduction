# Runtime assets

Run `python tools/download_calibration.py` from the repository root to place
the checksum-verified official RADIal `CalibrationTable.npy` in this directory.
The binary calibration table is intentionally excluded from Git.

